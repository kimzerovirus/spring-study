spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true

#logging:
#  level:
#    org.hibernate.SQL: debug # show_sql 은 콘솔에 남기는 반면 이렇게 설정하면 로그 파일로 남길 수 있다.
#    org.hibernate.type: trace