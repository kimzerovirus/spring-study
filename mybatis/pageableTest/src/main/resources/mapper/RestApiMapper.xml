<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.kzv.pageabletest.web.mapper.RestApiMapper">

<resultMap id="Content" type="me.kzv.pageabletest.web.dto.BoardResponseDto">
    <result property="idx" column="IDX" javaType="int"/>
    <result property="name" column="NAME" javaType="String"/>
    <result property="content" column="CONTENT" javaType="String"/>
    <result property="regDate" column="REG_DATE" javaType="String"/>
</resultMap>

    <select id="getBoardList"  resultType="Content">
        SELECT
            *
        FROM edu_memebers
        ORDER BY DESC;
    </select>
    <select id="getBoardTotal" resultType="Long">
        SELECT COUNT(idx) FROM edx_board
    </select>
</mapper>