spring.application.name: product-service
management:
  endpoints:
    web:
      base-path: / # http://localhost:8080/health
    enabled-by-default: false
  endpoint:
    health:
      enabled: true
      show-details: always
  health:
    db:
      enabled: false
    diskspace:
      enabled: true
